{
  "name": "you-will-pay",
  "version": "0.1.0",
  "devDependencies": {
    "bower": "^1.8.0",
    "concurrently": "^3.5.0",
    "env-cmd": "^7.0.0",
    "pulp": "^11.0.2",
    "purescript": "^0.11.6"
  },
  "scripts": {
    "deps": "stack setup && stack build --only-dependencies && npm install && bower install",
    "init_local_db": "sh init_db/init_local_db.sh",
    "server": "env-cmd env/test_remote_db stack build --exec ywp",
    "static-server": "pulp server",
    "pulp": "pulp",
    "start": "concurrently -r \"npm run server\" \"npm run static-server\""
  },
  "license": "UNLICENSED"
}
