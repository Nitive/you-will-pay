{
  "name": "you-will-pay",
  "version": "0.1.0",
  "devDependencies": {
    "bower": "^1.8.0",
    "concurrently": "^3.5.0",
    "pulp": "^11.0.2",
    "purescript": "^0.11.6"
  },
  "scripts": {
    "deps": "stack setup && stack build --only-dependencies && npm install && bower install",
    "init_local_db": "sh init_db/init_local_db.sh",
    "server": "DB_HOST=localhost ASSETS_PATH='http://localhost:1337/' stack build --exec ywp",
    "static-server": "pulp server",
    "pulp": "pulp",
    "start": "concurrently -r \"npm run server\" \"npm run static-server\""
  },
  "license": "UNLICENSED"
}
