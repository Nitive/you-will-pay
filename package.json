{
  "name": "you-will-pay",
  "version": "0.1.0",
  "devDependencies": {
    "bower": "^1.8.0",
    "concurrently": "^3.5.0",
    "pulp": "^11.0.2",
    "purescript": "^0.11.6"
  },
  "scripts": {
    "deps": "stack setup && stack install && npm install && bower install",
    "init_local_db": "sh init_db/init_local_db.sh",
    "server": "stack build --exec you-will-pay",
    "static-server": "pulp server",
    "pulp": "pulp",
    "start": "concurrently -r \"npm run server\" \"npm run static-server\""
  },
  "license": "UNLICENSED"
}
